# Edit before apply:
# - this is the only intended cluster-scope exception in the current design
# - bind it only to the sandbox-manager runtime identity in namespace `mbos`
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: agentsmith-manager-pv
rules:
  - apiGroups: [""]
    resources: ["persistentvolumes"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: agentsmith-manager-pv
subjects:
  - kind: ServiceAccount
    name: agentsmith-manager
    namespace: mbos
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: agentsmith-manager-pv
