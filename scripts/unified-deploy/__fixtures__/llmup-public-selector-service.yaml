apiVersion: v1
kind: Service
metadata:
  name: agentsmith-llmup-public
  labels:
    app.kubernetes.io/component: public-proxy
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: agentsmith
    app.kubernetes.io/component: llmup
  ports:
    - name: http
      port: 8080
      targetPort: http
