apiVersion: v1
kind: Service
metadata:
  name: agentsmith-llmup
  labels:
    app.kubernetes.io/component: llmup
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/component: llmup
  ports:
    - name: http
      port: 8080
      targetPort: http
