'use client';

import { EndpointsPageView, type EndpointsPageProps } from '@/components/endpoints/EndpointsPage';

export default function EndpointsPage(props: EndpointsPageProps) {
  return <EndpointsPageView {...props} />;
}
