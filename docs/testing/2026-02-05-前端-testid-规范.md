# 前端 data-testid 规范（v2）

日期：2026-02-05（最后更新：2026-04-11）

## 目标
- 提供稳定、可维护的测试选择器
- 避免依赖文案或样式导致的脆弱定位
- 保持与当前产品术语和 current IA 一致

## 命名规则
- 格式：`scope__element__state`
- 分隔符：双下划线 `__`
- 示例：
  - `login__submit`
  - `projects__create-btn`
  - `agents__table`
  - `page-state__error`
  - `notebook__task-header`

## 已实现的 testid 清单

### App Shell
| testId | 组件 | 说明 |
|--------|------|------|
| `topbar` | Topbar.tsx | 顶部导航栏 |
| `topbar__workspace-switcher` | Topbar.tsx | 工作空间切换器 |
| `topbar__project-switcher` | Topbar.tsx | 项目切换器 |
| `topbar__notifications` | NotificationCenter.tsx | 通知按钮 |
| `topbar__user-menu` | UserMenu.tsx | 用户菜单按钮 |
| `user-menu__profile` | UserMenu.tsx | 个人资料链接 |
| `user-menu__api-keys` | UserMenu.tsx | API Keys 链接 |
| `user-menu__workspace-integrations` | UserMenu.tsx | Workspace integrations 链接 |
| `user-menu__personal-connections` | UserMenu.tsx | Personal connections 链接 |
| `user-menu__language` | UserMenu.tsx | 语言切换 |
| `user-menu__logout` | UserMenu.tsx | 退出按钮 |
| `sidebar` | AppShellSidebar.tsx | 侧边栏容器 |
| `sidebar__nav-item--{name}` | AppShellSidebar.tsx | 导航项 |
| `sidebar__collapse-btn` | AppShellSidebar.tsx | 折叠按钮 |

### Login & Entry
| testId | 组件 | 说明 |
|--------|------|------|
| `login__workspace-select` | login/page.tsx | 工作空间选择 |
| `login__email-input` | login/page.tsx | 邮箱输入 |
| `login__keycloak-btn` | login/page.tsx | Keycloak 登录 |
| `login__submit` | login/page.tsx | 快捷登录按钮 |
| `workspace-select__heading` | workspace/page.tsx | 选择标题 |
| `workspace-select__card--{id}` | workspace/page.tsx | 工作空间卡片 |
| `join__accept-btn` | join/page.tsx | 接受邀请 |
| `join__decline-btn` | join/page.tsx | 拒绝邀请 |

### Projects
| testId | 组件 | 说明 |
|--------|------|------|
| `projects__search` | projects/page.tsx | 搜索框 |
| `projects__create-btn` | projects/page.tsx | 创建项目按钮 |
| `projects__table` | projects/page.tsx | 项目表格 |
| `projects__pin-btn` | projects/page.tsx | 置顶按钮 |

### Overview
| testId | 组件 | 说明 |
|--------|------|------|
| `overview__readiness-card--{type}` | overview/page.tsx | readiness 卡片 |
| `overview__time-range` | overview/page.tsx | 时间范围选择 |
| `overview__activity-timeline` | overview/page.tsx | 活动时间线 |

### CRUD Pages（统一模式）
每个 CRUD 页面遵循 `{scope}__table`, `{scope}__create-btn`, `{scope}__create-dialog` 模式：

- **Agents**: `agents__table`, `agents__create-btn`, `agents__create-dialog`, `agents__edit-dialog`
- **Endpoints**: `endpoints__table`, `endpoints__create-btn`, `endpoints__create-dialog`, `endpoints__edit-dialog`
- **Project secrets**: `credentials__table`, `credentials__create-btn`, `credentials__create-dialog`, `credentials__rotate-dialog`, `credentials__delete-dialog`
- **Members**: `members__table`, `members__invite-btn`, `members__invite-dialog`
- **Files**: `files__table`, `files__upload-btn`, `files__upload-dialog`, `files__delete-dialog`

### Audit & Usage
| testId | 组件 | 说明 |
|--------|------|------|
| `audit__table` | AuditTable.tsx | 审计表格 |
| `audit__filters` | AuditPage.tsx | 筛选控件 |
| `usage__table` | UsageTable.tsx | 用量表格 |
| `usage__filters` | UsagePage.tsx | 筛选控件 |

## 使用原则
- 必须稳定：不随文案/样式变化
- 必须唯一：同一页面同一 testid 不重复
- 必须覆盖关键路径：登录、项目入口、Chat、Notebook、治理入口、发布前高价值页面
- testid 命名应直接跟随当前产品术语与页面对象名
