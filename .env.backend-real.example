# Secrets and optional overrides for backend-real validation.
#
# Most non-secret defaults come from infra/runtime/presets.env.
# Keep this file minimal unless a backend-real check truly needs an override.

PRESET_ENDPOINT_API_KEY=
