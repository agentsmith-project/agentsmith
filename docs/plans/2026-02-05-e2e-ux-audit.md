# E2E UX Audit Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan.

**Goal:** Expand E2E coverage to visit all major zh-CN pages, capture key screenshots, and fix UI/UX issues while preserving the product’s core visual language and updating UXUI docs to reflect intentional improvements.

**Architecture:** Use Playwright to drive authenticated navigation via MSW + quick login, capture deterministic screenshots per page/state, and add lightweight UX guardrails (console error checks, overflow checks, and critical UI assertions). UI fixes are implemented in the relevant page/components and documented in UXUI specs where behavior or visuals are updated beyond current docs.

**Tech Stack:** Next.js App Router, Playwright, TailwindCSS, shadcn/ui, Zustand, MSW, UXUI docs in `/home/percy/works/mbos-server-v1/文档/UXUI/`.

---

### Task 1: Map required pages and baseline expectations

**Files:**
- Modify: `docs/plans/2026-02-05-e2e-ux-audit.md`
- Read: `/home/percy/works/mbos-server-v1/文档/UXUI/00-设计系统/视觉设计系统-v1.md`
- Read: `/home/percy/works/mbos-server-v1/文档/UXUI/03-页面规格/README.md`
- Read: `/home/percy/works/mbos-server-v1/文档/UXUI/03-页面规格/**`
- Read: `/home/percy/works/mbos-server-v1/文档/UXUI/01-通用规范/页面清单-模块与权限可见性-v1.md`

**Step 1: Write the failing test**

Add a failing expectation in `e2e/route-coverage.spec.ts` for missing major routes (join, login/workspace, userdata, credentials, user/profile, user/api-keys, settings) so the test fails until routes list is updated.

**Step 2: Run test to verify it fails**

Run: `npx playwright test e2e/route-coverage.spec.ts --project=chromium`
Expected: FAIL with missing routes in coverage list

**Step 3: Write minimal implementation**

Update `e2e/fixtures/routes.ts` (or the route list source used by coverage test) to include all major zh-CN routes from UXUI specs.

**Step 4: Run test to verify it passes**

Run: `npx playwright test e2e/route-coverage.spec.ts --project=chromium`
Expected: PASS

**Step 5: Commit**

```bash
git add e2e/route-coverage.spec.ts e2e/fixtures/routes.ts
git commit -m "test: expand route coverage for ux audit"
```

---

### Task 2: Expand screenshot capture to all major pages and states

**Files:**
- Modify: `e2e/capture-screenshots.spec.ts`
- Modify: `e2e/utils/navigation.ts` (if needed)

**Step 1: Write the failing test**

Add placeholder screenshot assertions (e.g., expected output paths count or route list coverage) in `e2e/capture-screenshots.spec.ts` to fail until new pages are added.

**Step 2: Run test to verify it fails**

Run: `npx playwright test e2e/capture-screenshots.spec.ts --project=chromium`
Expected: FAIL due to missing screenshot steps/paths

**Step 3: Write minimal implementation**

Update screenshot flow to include:
- `/zh-CN/login/workspace`
- `/zh-CN/join`
- `/zh-CN/workspaces/{ws}/projects/{prj}/userdata`
- `/zh-CN/workspaces/{ws}/projects/{prj}/credentials`
- `/zh-CN/user/profile`
- `/zh-CN/user/api-keys`
Add key states: members drawer, settings tabs, credentials create dialog, and any critical empty/disabled states required by specs.

**Step 4: Run test to verify it passes**

Run: `npx playwright test e2e/capture-screenshots.spec.ts --project=chromium`
Expected: PASS

**Step 5: Commit**

```bash
git add e2e/capture-screenshots.spec.ts e2e/utils/navigation.ts
git commit -m "test: capture all major pages and states"
```

---

### Task 3: Add UX guardrails for obvious UI defects

**Files:**
- Modify: `e2e/console-errors.spec.ts`
- Modify: `e2e/smoke.spec.ts` (or new `e2e/ux-guardrails.spec.ts`)

**Step 1: Write the failing test**

Add a new guardrail test that fails on:
- layout overflow (horizontal scroll) on key pages
- missing sidebar/topbar on project pages
- missing primary CTA on login page

**Step 2: Run test to verify it fails**

Run: `npx playwright test e2e/ux-guardrails.spec.ts --project=chromium`
Expected: FAIL on at least one page until UI fixes applied

**Step 3: Write minimal implementation**

Implement minimal selectors and assertions to validate:
- `document.documentElement.scrollWidth <= window.innerWidth` on key pages
- topbar + sidebar visible on project routes
- login primary action visible

**Step 4: Run test to verify it passes**

Run: `npx playwright test e2e/ux-guardrails.spec.ts --project=chromium`
Expected: PASS

**Step 5: Commit**

```bash
git add e2e/console-errors.spec.ts e2e/ux-guardrails.spec.ts
git commit -m "test: add ux guardrails for layout and shell"
```

---

### Task 4: UI/UX fixes based on screenshots and specs

**Files:**
- Modify: `src/app/[locale]/**/page.tsx`
- Modify: `src/components/**`
- Modify: `src/styles/**` or `src/app/[locale]/globals.css`

**Step 1: Write the failing test**

Add targeted E2E assertions for each identified issue (e.g., missing contrast, misaligned headers, missing section titles).

**Step 2: Run test to verify it fails**

Run: `npx playwright test e2e/ux-guardrails.spec.ts --project=chromium`
Expected: FAIL with the added assertions

**Step 3: Write minimal implementation**

Fix UI issues while preserving the core visual language (dark efficiency, restrained palette, AI accents). When intentional improvements diverge from current docs, update the UXUI specs to match the improved design.

**Step 4: Run test to verify it passes**

Run: `npx playwright test e2e/ux-guardrails.spec.ts --project=chromium`
Expected: PASS

**Step 5: Commit**

```bash
git add src/app src/components src/styles /home/percy/works/mbos-server-v1/文档/UXUI
git commit -m "fix: align key pages with improved ui"
```

---

### Task 5: Run full E2E sweep and update design docs

**Files:**
- Modify: `/home/percy/works/mbos-server-v1/文档/UXUI/03-页面规格/**`
- Modify: `/home/percy/works/mbos-server-v1/文档/UXUI/00-设计系统/视觉设计系统-v1.md` (only if global tokens/standards change)

**Step 1: Write the failing test**

Add a simple smoke expectation in `e2e/full-app.spec.ts` to ensure all target routes return 200 and the shell renders.

**Step 2: Run test to verify it fails**

Run: `npx playwright test e2e/full-app.spec.ts --project=chromium`
Expected: FAIL on any missing route or shell regression

**Step 3: Write minimal implementation**

Update UXUI specs for pages that received meaningful UI/UX improvements beyond existing documentation, ensuring doc updates reflect the new “best-in-class” design while still matching core design language.

**Step 4: Run test to verify it passes**

Run: `npx playwright test e2e/full-app.spec.ts --project=chromium`
Expected: PASS

**Step 5: Commit**

```bash
git add e2e/full-app.spec.ts /home/percy/works/mbos-server-v1/文档/UXUI
git commit -m "docs: sync ux specs with improved ui"
```

---

### Task 6: Final verification

**Files:**
- Modify: `test-results/screenshots/**` (generated)

**Step 1: Run full test suite**

Run: `npm test`
Expected: PASS

**Step 2: Run screenshot capture**

Run: `npx playwright test e2e/capture-screenshots.spec.ts --project=chromium`
Expected: PASS, screenshots generated under `test-results/screenshots/`

**Step 3: Commit**

No commit for generated screenshots (gitignored)

