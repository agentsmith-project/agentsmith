# OpenAPI Changelog

Baseline `origin/main` is not available locally.

## 2026-05-15

- Pre-GA file-library restore was simplified to the direct restore contract, so the old restore preview, restore run, and restore cancel operations are intentionally removed from the active product OpenAPI surface and registered in the breaking allowlist.
